# Implementation Plans

Generated by the improve skill (`/improve next` — direction-only audit) on
2026-06-12, at commit `a4acb59` (branch `tags`). Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Enforce monthly operation quotas and team size caps | P1 | M | — | TODO |
| 002 | Design spike: Lifecycle rules spec | P2 | M | — | TODO |
| 003 | PostHog Analytics Tracking | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 and 002 are independent and can run in parallel.
- 002 produces a design spec only; building lifecycle rules requires a
  follow-up implementation plan after the spec is reviewed.
- Both plans carry STOP conditions tied to `feat/editor-team-role` (a
  complete, unmerged branch on origin that changes team-role checks).
  Merging that branch first is recommended housekeeping.

## Verification baseline warning

At `a4acb59`, only `pnpm test` is a clean gate (53 files / 458 tests, ~3s).
`pnpm lint` fails with 27 pre-existing problems (12 errors, 15 warnings) and
`pnpm exec tsc --noEmit` fails with 10 pre-existing errors (stale generated
Prisma types missing `METADATA_CHANGE`, missing `archiver` types, an
undefined `useQuery` at `src/lib/queries/objects.ts:239`). Plans therefore
gate on "no new problems vs baseline". Cleaning this up was NOT planned (the
audit was direction-only) but is the obvious finding #1 for a future
correctness pass.

## Direction findings considered but not planned this round

- **Config/compliance health probes** (GetBucketEncryption,
  GetPublicAccessBlock, GetBucketLifecycleConfiguration missing from
  `src/lib/health/probes/bucket.ts`) — solid M-effort candidate; user chose
  not to plan it this round. Note: 002's spec includes a lifecycle probe,
  partially overlapping.
- **Public file requests** (inbound "send me files" inverse of share links)
  — grounded in mature share-link + multipart-presign infra; L effort,
  public-write abuse surface; not planned.
- **Team-shared bookmarks/notes** (`src/lib/db/bookmarks.ts:62,107` is
  userId-scoped despite team workspaces) — requires migration; not planned.
- **SSO / assume-role credential import** (`src/lib/aws/parse-profiles.ts:92-114`
  says "not yet supported") — large; not planned.
- **Connections export** (import exists at `/api/connections/import`, no
  export) — low value since secrets can't be exported; verdict: not worth
  doing standalone.

## Findings considered and rejected (do not re-audit)

Audit-subagent claims disproven by reading the code at `a4acb59`:

- "Direct-to-S3 uploads and CORS health probe are unbuilt" — both merged to
  main (PR #3; health commits `adfbb4d`…`1bc409c`).
- "Bookmark drag-reorder missing" — shipped (dnd-kit in
  `src/components/shared/app-sidebar.tsx`, `POST /api/bookmarks/reorder`).
- "Note counts not surfaced in browser" — shipped (`useNoteCounts` wired in
  `src/components/browser/file-browser.tsx:138`).
- "Search crawler has no UI" — shipped (command palette components,
  `connection-indexing-card.tsx`, `search-index-status.tsx`).
- "Activity feed is write-only" — false (`GET /api/activity`, drawer
  `activity-tab.tsx`, `overview-activity-card.tsx`).
